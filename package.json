{
  "name": "aws-cors-proxy-twitter",
  "version": "1.0.0",
  "description": "A simple CORS proxy for AWS lambda o similar services to access twitter API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}
